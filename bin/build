#!/usr/bin/env ruby

require 'bundler/setup'
require 'rubocop'
require 'yaml'

[:ruby, :rails].each do |type|
  config = RuboCop::ConfigLoader.load_file(".rubocop-#{type}.yml")

  File.open("dist/.rubocop-#{type}.yml", 'w') do |file|
    file.write(config.to_h.to_yaml)
  end
end
